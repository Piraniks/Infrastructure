jenkins:
  securityRealm:
    local:
      users:
      - id: "panda"
        name: "Panda"
        password: "panda"

  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:panda"

  numExecutors: 0
